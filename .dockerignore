# Node.js dependencies. We will run `npm install` inside the container.
node_modules
npm-debug.log

# C++ build artifacts. These are generated inside the container.
build
build-*

# Temporary session files created by the server.
temp_sessions

# Git repository files.
.git
.gitignore

# The Dockerfile itself is not needed inside the image.
Dockerfile

# Any local testing files or documentation.
README.md
test_program.c